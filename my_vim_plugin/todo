0. vimscripts refine 
1. SearchBar to find all definition in a predefine-folder
2. HighLight current word in current buffer by <shift + F8>

3. invoke a terminal window
3.1 preview window if current cursor located word is a file_name & the file exist.

4. ctrl + <- / -> : to select ---> then copy or delete.(vistual mode)

5. auto-complete : member_complete or prefix-string_complete
6. add current line to tag, jump back.
7. BUG - multi window | the ctags file not in current path 
