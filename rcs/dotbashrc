# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

# User specific aliases and functions
alias grep='grep --color=auto '
export PS1='\[\033k:\W\033\\\]'
export PS1="\n[\u@\h:\w]\n"$PS1'\$ '
